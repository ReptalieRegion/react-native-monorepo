import React from 'react';
import { View } from 'react-native';

type EntityMangerUpdatePageProps = {};

export default function EntityManagerUpdatePage({}: EntityMangerUpdatePageProps) {
    return <View />;
}
